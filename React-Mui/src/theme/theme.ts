import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#320D59",
    },
    secondary: {
      main: "#A482BC",
    },
    background: {
      default: "#e0d9d9",
    },
    text: {
      primary: "#200F3B",
      secondary: "#7b64a0",
    },
  },
});
